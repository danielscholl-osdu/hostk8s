# ShellCheck configuration for HostK8s
# Project-specific exclusions based on established codebase patterns

# SC1091: Not following sourced files (all scripts source common.sh)
# SC2155: Declare and assign separately (used consistently across 25+ locations)
# SC2034: Unused variables (color constants defined in common.sh but used by sourced scripts)
# SC2076: Remove quotes from RHS of =~ (used in validation functions pattern matching)
exclude=SC1091,SC2155,SC2034,SC2076

# Enable helpful checks
enable=require-variable-braces

# Set shell type
shell=bash

# External sources (allow following sourced files when possible)
external-sources=true
