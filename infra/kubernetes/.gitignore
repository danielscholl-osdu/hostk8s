# Ignore everything by default
/*

# Allow specific configurations
!/kind-custom.yaml
!/kind-config-minimal.yaml
!/kind-worker.yaml

# Allow README at root level
!/README.md

# Allow .gitignore itself
!/.gitignore
