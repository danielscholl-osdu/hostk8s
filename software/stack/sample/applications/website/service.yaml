apiVersion: v1
kind: Service
metadata:
  name: sample-website
  namespace: sample-website
  labels:
    hostk8s.application: website
spec:
  selector:
    app: sample-website
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
