apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cert-manager shared component for local development
# Provides certificate management with self-signed CA

resources:
  - namespace.yaml
  - source.yaml
  - release.yaml
  - cluster-issuer.yaml
