apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - source.yaml
  - release.yaml
  - external-secret.yaml
  - pgadmin-deployment.yaml
  - pgadmin-service.yaml
  - pgadmin-ingress.yaml

commonLabels:
  hostk8s.component: postgres
  component.hostk8s.io/type: foundation
  component.hostk8s.io/category: database
