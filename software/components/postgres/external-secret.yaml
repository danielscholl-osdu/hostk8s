# Generated ExternalSecret manifest for postgres/pgadmin4
# This file is auto-generated by manage-secrets.sh - safe to commit to Git
# Contains no sensitive data - only Vault path references
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pgadmin4-credentials
  namespace: hostk8s
  labels:
    hostk8s.io/managed: "true"
    hostk8s.io/contract: "sample-app"
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: pgadmin4-credentials
    creationPolicy: Owner
  data:
  - secretKey: email
    remoteRef:
      key: hostk8s/pgadmin4-credentials
      property: email
  - secretKey: password
    remoteRef:
      key: hostk8s/pgadmin4-credentials
      property: password
