apiVersion: apps/v1
kind: Deployment
metadata:
  name: kibana-status
  namespace: elasticsearch
  labels:
    hostk8s.app: kibana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kibana-status
  template:
    metadata:
      labels:
        app: kibana-status
        hostk8s.app: kibana
    spec:
      containers:
        - name: status
          image: busybox:1.36
          command: ['sleep', 'infinity']
          resources:
            requests:
              cpu: 1m
              memory: 8Mi
            limits:
              cpu: 1m
              memory: 8Mi
