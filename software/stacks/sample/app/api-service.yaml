apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: sample
  labels:
    hostk8s.application: api
spec:
  selector:
    app: sample-api
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
