apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sample-website-tls
  namespace: sample
  labels:
    hostk8s.application: website
spec:
  secretName: sample-website-tls-secret
  issuerRef:
    name: root-ca-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
  - localhost
