apiVersion: hostk8s.io/v1
kind: SecretContract
metadata:
  name: foundation/postgres
spec:
  secrets:
    - name: pgadmin4-credentials
      namespace: postgres
      data:
        - key: email
          value: admin@hostk8s.com
        - key: password
          generate: password
          length: 12
