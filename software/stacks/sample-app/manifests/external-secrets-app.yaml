# Generated ExternalSecret manifests for sample-app namespace
# This file is auto-generated by manage-secrets.sh - safe to commit to Git
# Contains no sensitive data - only Vault path references
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-credentials
  namespace: postgres
  labels:
    hostk8s.io/managed: "true"
    hostk8s.io/contract: "sample-app"
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-credentials
    creationPolicy: Owner
  data:
  - secretKey: username
    remoteRef:
      key: sample-app/postgres-credentials
      property: username
  - secretKey: database
    remoteRef:
      key: sample-app/postgres-credentials
      property: database
  - secretKey: host
    remoteRef:
      key: sample-app/postgres-credentials
      property: host
  - secretKey: port
    remoteRef:
      key: sample-app/postgres-credentials
      property: port
  - secretKey: url
    remoteRef:
      key: sample-app/postgres-credentials
      property: url
