# Generated ExternalSecret manifests from hostk8s.secrets.yaml
# This file is auto-generated by manage-secrets.py - safe to commit to Git
# Contains no sensitive data - only Vault path references
# To regenerate: make up sample-app

---
# ExternalSecret for postgres-credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-credentials
  namespace: sample-app
  labels:
    hostk8s.io/managed: 'true'
    hostk8s.io/contract: sample-app
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-credentials
    creationPolicy: Owner
  data:
  - secretKey: username
    remoteRef:
      key: sample-app/sample-app/postgres-credentials
      property: username
  - secretKey: database
    remoteRef:
      key: sample-app/sample-app/postgres-credentials
      property: database
  - secretKey: host
    remoteRef:
      key: sample-app/sample-app/postgres-credentials
      property: host
  - secretKey: port
    remoteRef:
      key: sample-app/sample-app/postgres-credentials
      property: port
  - secretKey: url
    remoteRef:
      key: sample-app/sample-app/postgres-credentials
      property: url

---
# ExternalSecret for redis-commander-credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: redis-commander-credentials
  namespace: redis
  labels:
    hostk8s.io/managed: 'true'
    hostk8s.io/contract: sample-app
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: redis-commander-credentials
    creationPolicy: Owner
  data:
  - secretKey: username
    remoteRef:
      key: sample-app/redis/redis-commander-credentials
      property: username
  - secretKey: password
    remoteRef:
      key: sample-app/redis/redis-commander-credentials
      property: password

---
# ExternalSecret for pgadmin4-credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pgadmin4-credentials
  namespace: hostk8s
  labels:
    hostk8s.io/managed: 'true'
    hostk8s.io/contract: sample-app
spec:
  refreshInterval: 10s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: pgadmin4-credentials
    creationPolicy: Owner
  data:
  - secretKey: email
    remoteRef:
      key: sample-app/hostk8s/pgadmin4-credentials
      property: email
  - secretKey: password
    remoteRef:
      key: sample-app/hostk8s/pgadmin4-credentials
      property: password
