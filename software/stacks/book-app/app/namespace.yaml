apiVersion: v1
kind: Namespace
metadata:
  name: bookinfo
  labels:
    # Enable Istio Ambient mode - instant mTLS without sidecars!
    istio.io/dataplane-mode: ambient
    # Stack identification
    hostk8s.stack: book-app
    hostk8s.app: bookinfo
