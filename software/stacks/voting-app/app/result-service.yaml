apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: voting-app
  labels:
    app: result
    hostk8s.app: voting-app
    hostk8s.component: result-service
spec:
  selector:
    app: result
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30082
    protocol: TCP
    name: http
  type: NodePort
