# HostK8s Environment Configuration
# Copy this file to .env and uncomment/modify as needed
# Most settings have sensible defaults - only override what you need

# =============================================================================
# LOGGING
# =============================================================================

# LOG_LEVEL=debug                         # Log level: debug, info, warn, error

# =============================================================================
# CLUSTER SETTINGS
# =============================================================================

# CLUSTER_NAME=hostk8s                   # Cluster name (default: hostk8s)
# K8S_VERSION=v1.33.2                    # Kubernetes version (default: latest)
# KIND_CONFIG=default                    # Kind config: minimal, simple, default

# Package manager preference for tool installation:
#   - Unset/empty: Auto-detect (prefer Homebrew, fallback to native)
#   - brew: Force Homebrew only
#   - native: Skip Homebrew, use native package manager (apt/apk)
# PACKAGE_MANAGER=brew

# =============================================================================
# INFRASTRUCTURE ADD-ONS
# =============================================================================

# FLUX_ENABLED=true                      # GitOps with Flux
# METALLB_ENABLED=false                  # Manual LoadBalancer support
# INGRESS_ENABLED=false                  # Manual NGINX Ingress Controller

# =============================================================================
# GITOPS CONFIGURATION (when FLUX_ENABLED=true)
# =============================================================================

# GITOPS_REPO=https://community.opengroup.org/danielscholl/hostk8s
# GITOPS_BRANCH=main                     # Git branch to sync
# SOFTWARE_STACK=sample                  # Stack to deploy: sample, sample-stack

# =============================================================================
# AI-ASSISTED GITOPS (MCP INTEGRATION)
# =============================================================================

# FLUX_MCP_ENABLED=true                  # Enable Flux MCP server for AI assistance
# FLUX_MCP_READ_ONLY=false               # Set to true for read-only operations
